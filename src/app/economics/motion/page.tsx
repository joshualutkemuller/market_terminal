import { MotionStudio } from "@/components/econ/MotionStudio";

/** `MOTN` — Macro Motion Studio: animate user-selected economic series over time. */
export default function MacroMotionPage() {
  return <MotionStudio />;
}
